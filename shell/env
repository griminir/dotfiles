# ~/.config/shell/env

path_prepend() {
  case ":$PATH:" in
    *":$1:"*) ;; # already there
    *) PATH="$1:$PATH" ;;
  esac
}

[ -d "$HOME/bin" ] && path_prepend "$HOME/bin"
[ -d "$HOME/.local/bin" ] && path_prepend "$HOME/.local/bin"

export PATH
